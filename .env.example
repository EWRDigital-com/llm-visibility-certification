# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Firecrawl API key — powers the page scrape in the Phase 0 CLI (scripts/score-*).
# Get one at https://firecrawl.dev. Matt's shared key lives in seo-intel/.env.
FIRECRAWL_API_KEY=
